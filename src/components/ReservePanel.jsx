export default function ReservePanel() {
  return (
    <div style={{ padding: 24, textAlign: "center", color: "var(--text-secondary)" }}>
      <p>Panel de Reservas — próximamente.</p>
    </div>
  );
}
